version: "3.8"

services:
  app:
    # Local development override: publish host port to access the app on localhost
    ports:
      - "${HOST_PORT:-3000}:3000"

networks:
  portfolio_net:
    driver: bridge
